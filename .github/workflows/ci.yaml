name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.sha }}
  cancel-in-progress: true

env:
  RUST_BACKTRACE: 1
  RUST_MIN_STACK: 16777216
  # Change to specific Rust release to pin or `stable` for the latest stable version.
  rust_stable: "1.88"
  rust_nightly: nightly

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target:
          - x86_64-unknown-linux-gnu
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
      - name: Install Rust ${{ env.rust_stable }}
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ env.rust_stable }}
          targets: ${{ matrix.target }}
      - name: Install Target
        run: rustup target add ${{ matrix.target }}
      - uses: Swatinem/rust-cache@v2
      - name: Build
        run: rustup target add x86_64-unknown-linux-gnu && cargo build --workspace --release --target ${{ matrix.target }}

  build_wasm:
    name: Build WASM
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target:
          - wasm32-unknown-unknown
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
      - name: Install Rust ${{ env.rust_stable }}
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ env.rust_stable }}
          targets: ${{ matrix.target }}
      - name: Install Target
        run: rustup target add ${{ matrix.target }}
      - uses: Swatinem/rust-cache@v2
      - name: Build
        run: rustup target add wasm32-unknown-unknown && RUSTFLAGS='--cfg getrandom_backend="wasm_js"' cargo build --workspace --release --target ${{ matrix.target }}

  fmt:
    name: Format Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v5
      - name: Install Rust ${{ env.rust_nightly }}
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ env.rust_nightly }}
          components: rustfmt
      - name: Use Rust Cache
        uses: Swatinem/rust-cache@v2
      - name: Fmt Check
        run: rustup component add --toolchain 1.88-x86_64-unknown-linux-gnu rustfmt && cargo fmt --all -- --check

  test:
    name: Test Suite
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v5
      - name: Install Rust ${{ env.rust_stable }}
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ env.rust_stable }}
      - name: Use Rust Cache
        uses: Swatinem/rust-cache@v2
      - name: Run Tests
        # The three crates carrying the INSECURE `unsafe_mock` feature — twopc_mpc,
        # class_groups, mpc — are tested WITHOUT it and are excluded from the
        # `--all-features` run: `--all-features` would activate `unsafe_mock` (which
        # short-circuits proof verification and swaps in deterministic keygen/protocol
        # mocks), so the suite would exercise the mocks instead of the real crypto and
        # lose all real coverage — defeating the point of the tests. Each is tested
        # separately with its real feature set. The mock mode itself is validated
        # downstream in ika; locally run e.g.
        # `cargo test -p twopc_mpc --features unsafe_mock --release`.
        run: |
          cargo test --all-features --workspace --exclude twopc_mpc --exclude class_groups --exclude mpc --release
          cargo test -p twopc_mpc --features benchmarking,parallel --release
          cargo test -p class_groups --features benchmarking,randomized_decryption --release
          cargo test -p mpc --features test_helpers,parallel --release

  clippy:
    name: Clippy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v5
      - name: Install Rust ${{ env.rust_nightly }}
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ env.rust_nightly }}
          components: clippy
      - name: Use Rust Cache
        uses: Swatinem/rust-cache@v2
      - name: Run Clippy
        run: rustup component add --toolchain 1.88-x86_64-unknown-linux-gnu clippy && cargo clippy --all --tests --all-targets --all-features --no-deps -- -D warnings

  docs:
    name: Docs
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v5
      - name: Install Rust ${{ env.rust_stable }}
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ env.rust_stable }}
      - name: Use Rust Cache
        uses: Swatinem/rust-cache@v2
      - name: Check Documentation
        # --no-deps = Do not build documentation for dependencies.
        # --workspace = Document all members in the workspace.
        # --all-features = Activate all available features of all selected packages.
        # --document-private-items = Include non-public items in the documentation.
        # This will be enabled by default if documenting a binary target.
        # --examples = Document all example targets.
        run: |
          cargo doc --workspace --no-deps --all-features --document-private-items --examples

  verify-license:
    name: License Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v5
      - name: Verify Repository License
        run: |
          chmod +x "${{ github.workspace }}/.github/scripts/verify-license-rust.sh"
          chmod +x "${{ github.workspace }}/.github/scripts/verify-license-presence.sh"
          "${{ github.workspace }}/.github/scripts/verify-license-rust.sh"
          "${{ github.workspace }}/.github/scripts/verify-license-presence.sh"
